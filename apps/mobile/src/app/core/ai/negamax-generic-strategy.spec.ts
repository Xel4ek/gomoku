import { NegamaxGenericStrategy } from './negamax-generic-strategy';

describe('NegamaxGenericStrategy', () => {
  it('should create an instance', () => {
    expect(new NegamaxGenericStrategy()).toBeTruthy();
  });
});
